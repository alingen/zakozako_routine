# ざこざこルーティン

習慣化アプリ。
ユーザーが毎日の習慣を達成しながら、キャラクター「莉央」と交流する。

## Product Principle

このアプリでは「毎日莉央に会いに来る」体験を重視する。

一般的な無機質な習慣管理アプリにはしない。
ただし、キャラクター性を優先して操作性を犠牲にしてはいけない。

## Design

### 色

定数は `MesugakiRoutine/DesignSystem/AppColor.swift`。コードでは必ず `AppColor` の定数を使う。

ブランドカラー:

| 名前 | 値 | 定数 | 役割 |
|---|---|---|---|
| Primary | #D73A5A | `primary` | 「今押すべき操作」と莉央（完了ボタン、追加、NEW、名札） |
| Pink | #F8D5DC | `primarySoft` | 莉央の吹き出し、アイコンの丸い地 |
| Purple | #735ECF | `secondary` | ストーリー・思い出、達成のお知らせなど特別感のあるもの |
| Yellow | #FFD166 | `accent` | 連続記録の炎などの飾り |
| Background | #FFF7F3 | `background` | 画面の地 |
| Surface | #FFFFFF | `surface` | カード、入力欄、読みもの画面の地 |
| Text | #352C32 | `text` | 本文、見出し |
| Muted | #81737A | `muted` | 補足、未選択 |
| Border | #EADCE0 | `border` | 枠線、区切り線 |

状態表示専用の色（状態を示すときだけ使い、装飾やブランド表現には使わない）:

| 名前 | 値 | 定数 | 役割 |
|---|---|---|---|
| Success | #3F8F70 | `success` | 達成・完了 |
| Warning | #B66A13 | `warning` | 注意・警告 |
| Error | #B93847 | `error` | 失敗・削除・エラー |

使い方のルール:

- Primary（赤）は「今押すべき操作」と莉央に限る。シェブロンや飾りのアイコンには使わない（muted か Purple にする）
- 文字の色と地の組み合わせは、コントラスト 4.5:1 以上を守る
  - Muted の文字は白地（Surface）の上だけ（白地 4.50:1、背景色の上では 4.25:1 で不足）
  - 背景色（Background）の上の補足文は Text にする
  - Yellow は文字や単独のアイコンに使わない（白地 1.44:1）。飾りとしてだけ使う
  - Success と Warning は文字に使わない（白地 3.91:1 / 4.15:1）。アイコンにだけ使い、添える文字は Text にする
  - Error は文字にも使える（白地 5.65:1）
- 画面の地: 通常の画面は Background の上に白いカード。ログや読了画面など読みもの・状態の画面は Surface
- 黒・白は直接指定してよい（ADVや交流画面の暗幕・操作バー、写真やカラー地の上の白文字など）

デザイン方針:

- キャラクターを主役にする
- 柔らかく、かわいい
- 少し生意気・いたずらっぽい
- 日本の商用スマートフォンアプリ程度の完成度を目標とする
- AI生成UIにありがちな過剰なグラデーション、カード、シャドウを避ける
- 新しい装飾を追加するより、余白・階層・配置・サイズの改善を優先する

## UI変更ルール

- ユーザーから指定されていない機能追加をしない
- ブランドカラーを勝手に変更しない
- キャラクター素材を加工しない
- ナビゲーション構造を勝手に変更しない
- UIライブラリを勝手に追加しない
